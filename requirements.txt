# Local / Docker Python deps for hrms-py
# Full ML stack (api.py) — also used by Dockerfile.
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
pymupdf>=1.24
spacy>=3.7
opencv-python-headless>=4.8
easyocr>=1.7
psutil>=5.9
python-docx>=1.1
numpy>=1.26
pydantic>=2.0

# After install, download the spaCy model used by ats_main:
#   python -m spacy download en_core_web_md
#
# Dev stub (no ML):
#   pip install fastapi uvicorn python-multipart pydantic
#   python -m uvicorn api_stub:app --reload --port 3001
